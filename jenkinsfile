pipeline {
    agent any

    stages {
        stage('HelloWorld') {
            steps {
                echo '*Hello World*'
            }
        }
        
        stage('Docker Build') {
            steps {
                sh 'docker pull nginx'
            }
        }

        stage('Docker Run') {
            steps {
                sh 'docker run -d -p 80:80 nginx'
            }
        }

        stage('Docker Image and PS') {
            steps {
                sh 'docker image'
                sh 'docker ps'
            }
        }
        stage('Docker Image and PS') {
            steps {
                sh 'docker stop nginx'
                sh 'docker prune -a'
            }
        }
    }
}
